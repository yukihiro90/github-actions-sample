name: Set End Date on Done

on:
  issues:
    types: [edited, closed]

jobs:
  update-end-date:
    runs-on: ubuntu-latest
    steps:
      - name: Check if issue is moved to Done and set end_date
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.MY_PAT }}
          script: |
            const issueId = context.payload.issue?.node_id;
            if (!issueId) return;

            const projectTitle = 'Test1';

            // issueのプロジェクトを取得
            const issue = await github.graphql(`
              query {
                node(id: "${issueId}") {
                  ... on Issue {
                    projectItems(first: 10) {
                      nodes {
                        project {
                          id,
                          title
                        },
                        fieldValueByName(name: "Status") {
                          ... on ProjectV2ItemFieldSingleSelectValue {
                            name
                          }
                        }
                      }
                    }
                  }
                }
              }
            `);
            console.dir(issue, {depth: null});
            const projects = issue.node.projectItems.nodes

